# Foodly Print Mobile — Releases

Descargas públicas (APK) de la **app móvil de impresión de Foodly**.

- Código fuente: repo privado `print-server-mobile`.
- Cada versión se publica aquí automáticamente desde el CI al hacer un *tag push* (`vX.Y.Z`).
- La app consulta este repo (`releases/latest`) y se **auto-actualiza**.